# remo-help-queue-alerter
Userscript to manually inject into remo that beeps and alerts whenever the help queue changes.

## Usage:
Copy the contents of `script.js` and paste into the console of remo.
